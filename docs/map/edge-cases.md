# Edge Cases:

**Potential edge cases to consider:**
- Nested languages (e,g,. JavaScript inside HTML, React.js, etc.)
- Comment syntax within strings (e,g,. "// text".)
- Scanning massive codebases for the initial scan.
- Comments that are not for the project (e,g,. `node_modules` files.)
- Deleting a file or even a directory.